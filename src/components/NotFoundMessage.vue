<template>
    <div class="flex flex-col justify-center absolute w-full h-full -mx-8 px-8">
        <h1 class="mb-0 font-hkgrotesk-semibold">{{ $t('not_found.title') }}</h1>
        <p class="mb-0">{{ $t('not_found.explain') }}</p>
        <p class="mb-0">{{ $t('not_found.details') }}</p>
        <p class="mb-0">{{ $t('not_found.tip') }}</p>
    </div>
</template>
